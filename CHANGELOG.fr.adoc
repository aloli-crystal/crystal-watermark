= Journal des modifications
:toc: left
:toc-title: Versions
:toclevels: 2

== v0.1.2 — 2026-04-29

=== Modifié

* **Repo GitHub renommé** vers `aloli-crystal/watermark` (convention
  de nommage Aloli : les créations originales abandonnent le préfixe
  `crystal-`). Les anciens liens redirigent automatiquement.
* Champ `name:`, cible de compilation et binaire CLI alignés sur le
  nouveau nom du repo. Nouveau shim d'entrée `src/watermark.cr`
  (charge `src/crystal_watermark.cr`, le code).
* Dépendance vers PDF mise à jour sur la nouvelle URL
  (`aloli-crystal/pdf`). `require "crystal-pdf/src/pdf"` dans `src/`
  mis à jour vers `require "pdf/src/pdf"`.
* Le module interne `Watermark` et le dossier `src/crystal_watermark/`
  restent inchangés — nettoyage reporté.

== v0.1.1 — 2026-04-25

Resserre la suite de tests d'intégration maintenant que
`pdf` v0.3.4 corrige le bug d'offset octet-vs-caractère dans
`find_xref_offset`.

* Les specs d'intégration vérifient maintenant à travers le vrai
  `PDF::Reader.open(dst)` (nombre de pages + MediaBox par page) au
  lieu des contournements byte-level de la v0.1.0. La sortie de
  chaque style est aussi re-parsée pour confirmer qu'elle produit
  un PDF syntaxiquement valide.
* Aucun changement de comportement côté utilisateur — la sortie du
  watermarker est byte-identique entre la v0.1.0 et la v0.1.1.

== v0.1.0 — 2026-04-25

Première version publique. Filigranage textuel en Crystal pur sur
fichiers PDF et image, conçu pour marquer les pièces d'identité et
documents confidentiels avant transmission. Inspiré du cas d'usage
de https://filigrane.beta.gouv.fr/[filigrane.beta.gouv.fr].

* **Filigranage PDF** via `PDF::Reader.add_content_stream` de
  pdf : injecte un content stream de filigrane sur chaque
  page, en préservant le contenu original. Supporte tout format de
  page (A4, Letter, A3, …) en lisant la `MediaBox` réelle de chaque
  page.
* **Filigranage d'images** pour les entrées PNG et JPEG (toujours
  réécrites en PNG) : une police bitmap est rendue pixel par pixel
  avec l'opacité demandée dans l'image source.
* **Cinq styles** : `Diagonal` (auto-dimensionné sur la diagonale,
  par défaut), `Tiled` (grille répétée), `Header`, `Footer`,
  `Center`.
* **CLI** dans `bin/crystal-watermark` avec drapeaux courts et longs
  pour toutes les options (`-t`, `-o`, `-s`, `--opacity`, `--color`,
  `--font-size`, `--rotation`, `-v`, `-h`).
* **API** : `CrystalWatermark.apply(input, output, text, style, options)`
  dispatche automatiquement selon l'extension du fichier.
* **Table de correspondance WinAnsiEncoding** pour les accents
  français/allemands/espagnols et la ponctuation typographique
  courante (tiret cadratin, guillemets typographiques, …).
* README bilingue (EN + FR) et CHANGELOG.
* CI : lint ameba + crystal spec à chaque push.
* **19 tests unitaires** couvrant les options, le filigranage
  d'image (5 styles, vérification de modification des pixels,
  accents, chemins d'erreur) et l'API publique.

== v0.0.x — 2026-04-15 et antérieur (non publié)

Itérations internes. Non publiées sur GitHub.
