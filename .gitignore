/docs/api/
/lib/
/bin/*
!/bin/.keep
/.shards/
*.dwarf
/shard.lock

# Test artefacts
/spec/tmp/

# PDFs de test posés à la racine ou dans tmp/ (PDFs canoniques
# pour les specs sont dans /spec/fixtures/, qui n'est PAS ignoré).
/*.pdf
/tmp/*.pdf

# macOS
.DS_Store

# Editors
*.swp
*~
